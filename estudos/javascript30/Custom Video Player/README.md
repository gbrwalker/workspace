# Custom Video Player

O arquivo de vídeo de demonstração não é versionado (binário pesado). Para testar o
player localmente, coloque um arquivo `.mp4` nesta pasta e ajuste o `src` da tag
`<video>` em `index.html`.
